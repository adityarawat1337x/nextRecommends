import { Transaction } from 'neo4j-driver';
export interface TransactionState {
    transaction: Transaction;
    run: Function;
    rollback: Function;
}
export declare const useTransaction: (defaultAccessMode: any, database?: string | undefined) => TransactionState;
export declare const useReadTransaction: (database?: string | undefined) => TransactionState;
export declare const useWriteTransaction: (database?: string | undefined) => TransactionState;
